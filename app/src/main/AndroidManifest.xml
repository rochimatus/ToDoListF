<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolistf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SoftBlue"
        android:fullBackupContent="true">
        <activity android:name=".modul.Home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modul.CreateTask.CreateTaskActivity"></activity>
        <activity android:name=".modul.ShowTask.ShowTaskActivity"></activity>
        <activity android:name=".modul.EditTask.EditTaskActivity"></activity>
    </application>

</manifest>